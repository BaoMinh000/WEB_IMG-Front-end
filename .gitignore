# Node modules
node_modules/

# Log files
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Env files (chứa thông tin nhạy cảm)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Build thư mục (sinh ra khi build dự án)
build/

# Các tệp cache
.cache/
.eslintcache

# Coverage thư mục (nếu dùng testing tools như Jest)
coverage/

# Tệp lock (có thể bỏ qua nếu không cần đồng bộ phụ thuộc)
yarn.lock
package-lock.json

# OS-specific files
.DS_Store
Thumbs.db

# IDE-specific files (VSCode, IntelliJ, etc.)
.vscode/
.idea/

# Tệp cấu hình hoặc tạm thời
*.log
*.tmp
*.swp
